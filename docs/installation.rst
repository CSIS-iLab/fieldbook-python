.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install fieldbook

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fieldbook
    $ pip install fieldbook
